const add = function (x, y)
{
    return x + y;
}

function add(x, y)
{
    return x + y;
}

const add = (x, y) =>
{
    return x + y;
}

const sqaure = x =>
{
    return x * x;
}

const cube = x =>
{
    return x ** 3;
}

const rollDie = () =>
{
    return Math.floor(Math.random() * 6) + 1;
}